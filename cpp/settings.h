#pragma once
#include <string>
using namespace std;

// number of iterations for each heuristic
static int NUMBER_OF_ITERATIONS = 10;

// size of puzzle (P x P)
static const int PUZZLE_SIZE = 4;

// name of files to save results (logs and path)
static string RUN_NAME = "size_4_iter_10";
